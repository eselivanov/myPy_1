import odbchelper
print odbchelper.__name__

""" __name__
    (1) attribute that contains name of file when it's imported and
    (2) __main__ when module is invoked as a standalone program,
        __name == "__main__"

     It can be used in if statement for debug
"""
